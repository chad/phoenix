/**
 * Web Client — Generated Tests
 *
 * AUTO-GENERATED by Phoenix VCS
 * Tests module structure, server health, and Phoenix traceability.
 */

import { describe, it, expect, afterAll } from 'vitest';
import { startServer } from '../server.js';

import * as gameBoardUi from '../game-board-ui.js';
import * as gameStatusDisplay from '../game-status-display.js';
import * as lobby from '../lobby.js';
import * as styling from '../styling.js';

describe('Web Client modules', () => {
  describe('Game Board UI', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(gameBoardUi._phoenix).toBeDefined();
      expect(gameBoardUi._phoenix.name).toBe('Game Board UI');
      expect(gameBoardUi._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(gameBoardUi).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Game Status Display', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(gameStatusDisplay._phoenix).toBeDefined();
      expect(gameStatusDisplay._phoenix.name).toBe('Game Status Display');
      expect(gameStatusDisplay._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(gameStatusDisplay).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Lobby', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(lobby._phoenix).toBeDefined();
      expect(lobby._phoenix.name).toBe('Lobby');
      expect(lobby._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(lobby).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Styling', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(styling._phoenix).toBeDefined();
      expect(styling._phoenix.name).toBe('Styling');
      expect(styling._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(styling).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

});

describe('Web Client server', () => {
  const instance = startServer(0); // random port

  afterAll(() => new Promise<void>(resolve => instance.server.close(() => resolve())));

  it('GET /health returns 200', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/health`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(body.status).toBe('ok');
    expect(body.service).toBe('Web Client');
  });

  it('GET /metrics returns request counts', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/metrics`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(typeof body.requests_total).toBe('number');
  });

  it('GET /modules lists all registered modules', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/modules`);
    expect(res.status).toBe(200);
    const body = await res.json() as Array<Record<string, unknown>>;
    expect(body.length).toBe(4);
  });

  it('GET /unknown returns 404', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/unknown`);
    expect(res.status).toBe(404);
  });

  it('GET / serves HTML page', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/`);
    expect(res.status).toBe(200);
    const ct = res.headers.get('content-type') ?? '';
    expect(ct).toContain('text/html');
    const body = await res.text();
    expect(body).toContain('<!DOCTYPE html>');
    expect(body).toContain('<title>Web Client</title>');
  });
});
