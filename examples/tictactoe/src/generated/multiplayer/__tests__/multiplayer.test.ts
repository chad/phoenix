/**
 * Multiplayer — Generated Tests
 *
 * AUTO-GENERATED by Phoenix VCS
 * Tests module structure, server health, and Phoenix traceability.
 */

import { describe, it, expect, afterAll } from 'vitest';
import { startServer } from '../server.js';

import * as gameRooms from '../game-rooms.js';
import * as matchmaking from '../matchmaking.js';
import * as playerManagement from '../player-management.js';
import * as realTimeCommunication from '../real-time-communication.js';

describe('Multiplayer modules', () => {
  describe('Game Rooms', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(gameRooms._phoenix).toBeDefined();
      expect(gameRooms._phoenix.name).toBe('Game Rooms');
      expect(gameRooms._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(gameRooms).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Matchmaking', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(matchmaking._phoenix).toBeDefined();
      expect(matchmaking._phoenix.name).toBe('Matchmaking');
      expect(matchmaking._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(matchmaking).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Player Management', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(playerManagement._phoenix).toBeDefined();
      expect(playerManagement._phoenix.name).toBe('Player Management');
      expect(playerManagement._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(playerManagement).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Real-Time Communication', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(realTimeCommunication._phoenix).toBeDefined();
      expect(realTimeCommunication._phoenix.name).toBe('Real-Time Communication');
      expect(realTimeCommunication._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(realTimeCommunication).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

});

describe('Multiplayer server', () => {
  const instance = startServer(0); // random port

  afterAll(() => new Promise<void>(resolve => instance.server.close(() => resolve())));

  it('GET /health returns 200', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/health`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(body.status).toBe('ok');
    expect(body.service).toBe('Multiplayer');
  });

  it('GET /metrics returns request counts', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/metrics`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(typeof body.requests_total).toBe('number');
  });

  it('GET /modules lists all registered modules', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/modules`);
    expect(res.status).toBe(200);
    const body = await res.json() as Array<Record<string, unknown>>;
    expect(body.length).toBe(4);
  });

  it('GET /unknown returns 404', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/unknown`);
    expect(res.status).toBe(404);
  });
});
