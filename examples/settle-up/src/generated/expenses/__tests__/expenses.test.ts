/**
 * Expenses — Generated Tests
 *
 * AUTO-GENERATED by Phoenix VCS
 * Tests module structure, server health, and Phoenix traceability.
 */

import { describe, it, expect, afterAll } from 'vitest';
import { startServer } from '../server.js';

import * as balanceCalculation from '../balance-calculation.js';
import * as creatingAnExpense from '../creating-an-expense.js';
import * as expenseHistory from '../expense-history.js';
import * as splitStrategies from '../split-strategies.js';

describe('Expenses modules', () => {
  describe('Balance Calculation', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(balanceCalculation._phoenix).toBeDefined();
      expect(balanceCalculation._phoenix.name).toBe('Balance Calculation');
      expect(balanceCalculation._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(balanceCalculation).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Creating an Expense', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(creatingAnExpense._phoenix).toBeDefined();
      expect(creatingAnExpense._phoenix.name).toBe('Creating an Expense');
      expect(creatingAnExpense._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(creatingAnExpense).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Expense History', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(expenseHistory._phoenix).toBeDefined();
      expect(expenseHistory._phoenix.name).toBe('Expense History');
      expect(expenseHistory._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(expenseHistory).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Split Strategies', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(splitStrategies._phoenix).toBeDefined();
      expect(splitStrategies._phoenix.name).toBe('Split Strategies');
      expect(splitStrategies._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(splitStrategies).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

});

describe('Expenses server', () => {
  const instance = startServer(0); // random port

  afterAll(() => new Promise<void>(resolve => instance.server.close(() => resolve())));

  it('GET /health returns 200', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/health`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(body.status).toBe('ok');
    expect(body.service).toBe('Expenses');
  });

  it('GET /metrics returns request counts', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/metrics`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(typeof body.requests_total).toBe('number');
  });

  it('GET /modules lists all registered modules', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/modules`);
    expect(res.status).toBe(200);
    const body = await res.json() as Array<Record<string, unknown>>;
    expect(body.length).toBe(4);
  });

  it('GET /unknown returns 404', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/unknown`);
    expect(res.status).toBe(404);
  });
});
