{
  "name": "trail",
  "version": "0.1.0",
  "description": "Generated by Phoenix VCS — 3 services",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start:board": "tsc && node dist/generated/board/server.js",
    "start:issues": "tsc && node dist/generated/issues/server.js",
    "start:sprints": "tsc && node dist/generated/sprints/server.js",
    "start": "tsc && node dist/generated/board/server.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
