/**
 * Sprints — Generated Tests
 * AUTO-GENERATED by Phoenix VCS
 */

import { describe, it, expect } from 'vitest';
import sprint_rollup from '../sprint-rollup.js';
import sprints from '../sprints.js';

describe('Sprints modules', () => {
  describe('Sprint Rollup', () => {
    it('exports a Hono router as default', () => {
      expect(sprint_rollup).toBeDefined();
      expect(typeof sprint_rollup.fetch).toBe('function');
    });
  });
  describe('Sprints', () => {
    it('exports a Hono router as default', () => {
      expect(sprints).toBeDefined();
      expect(typeof sprints.fetch).toBe('function');
    });
  });
});
